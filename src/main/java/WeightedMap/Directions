最小生成树问题：V-1条边连接V个顶点，使得V个顶点的权重和最小
最小生成树问题是针对无向图的
在此默认图只有一个连通分量

切分定理：给定任意切分，横切边中权值最小的边必然属于最小生成树

迪杰斯特拉算法：图中不能有负权边，迪杰斯特拉算法对有向图和无向图都成立

当一个图拥有负权环的时候，就不存在最短路径了

Bellman-ford只能解决途中有负权边不能有负权环的情况
但是能够检测图中是否有负权环，时间复杂度O(EV)
基本思想：如果一个图没有负权环，从一个顶点到另一个顶点，最多经过V个顶点，有V-1条边
否则，存在顶点经过两次，此时存在负权环


